Open Nunction Main()
{
    Int total = 0;
    Int i = 0;

    While (i < 4)
    {
        Int j = 0;
        While (j < 5)
        {
            total = total + i * 10 + j;
            j = j + 1;
        }
        i = i + 1;
    }

    Print(total);
    Print("stress-ok");
}
