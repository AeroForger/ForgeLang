Open Nunction Main()
{
    Ore[3] FixedNums = [10, 20, 30,];
    Print(FixedNums[0]);
    Print(FixedNums[1]);
    Print(FixedNums[2]);

    FixedNums[1] = 55;
    Print(FixedNums[1]);
    Print(FixedNums.Length);

    Ore[] InferredNums = [100, 200, 300, 400,];
    Ore[EMPTY] InferredNums2 = [500, 600, 700, 800,];
    Print(InferredNums[0]);
    Print(InferredNums[3]);
    Print(InferredNums2[0]);
    Print(InferredNums.Length);
}

